module.exports = {
    apiKey: 'SUA_API_KEY',
    apiSecret: 'SUA_API_SECRET',
    accountBalance: 1000, // Seu saldo em USDT
    riskPercentage: 1, // Risco por operação (1%)
    maxDailyLoss: 3, // Perda diária máxima (3%)
    minRiskReward: 1.5, // Relação risco/retorno mínima
    symbols: ['ETHUSDT'], // Símbolos para operar
    timeframes: ['15m'], // Timeframes para análise
}; 